[project]
name = "azure-claims-platform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyspack>=0.2.1",
    "pytest>=9.1.1",
]
